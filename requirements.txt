# Dependencias del núcleo (Fase 1)
numpy>=1.24
pandas>=2.0
PyYAML>=6.0

# Machine Learning (Fase 4). Opcionales: el sistema funciona sin ellas con el
# modelo logístico en numpy puro, pero se recomiendan en producción.
scikit-learn>=1.3
xgboost>=2.0

# Desarrollo / tests
pytest>=7.4

# Persistencia MySQL (Fase 6). Opcional: el backend por defecto es SQLite
# (librería estándar). Necesario solo para el dashboard PHP en producción.
# PyMySQL>=1.1

# --- Fases posteriores (descomentar cuando corresponda) ---
# MetaTrader5>=5.0        # Fase 3 (solo Windows con terminal MT5)
# tensorflow>=2.15        # Fase 4 (LSTM secuencial) — o torch como alternativa
